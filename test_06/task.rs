// 問題: 文字列ベクターを整形してからソート・重複排除してください。
// - `items` の各要素から前後の空白を `trim()` で取り除き、小文字化します。
// - 空文字列になったものは捨てます。
// - すべての要素をアルファベット順にソートし、重複を 1 件にまとめた Vec を返します。
// - 所有権を引き受けてよいので `items.into_iter()` を活用してください。`sort` / `dedup` などの標準メソッドで仕上げます。
pub fn normalize_and_sort(items: Vec<String>) -> Vec<String> {
    todo!("ここに処理を書いてください")
}
