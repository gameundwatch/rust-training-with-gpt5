// 問題: スライディングウィンドウの合計を計算してください。
// - `values` の連続した `window` 要素ごとの合計を Vec として返します。
// - `window` が 0、または values の長さより大きい場合は空の Vec を返してください。
// - `slice.windows(window)` や自前のループを使って、借用を崩さずに合計を求めましょう。
// - 合計は i64 で管理し、オーバーフローしない範囲でテストします。
pub fn sliding_window_sum(values: &[i64], window: usize) -> Vec<i64> {
    todo!("ここに処理を書いてください")
}
