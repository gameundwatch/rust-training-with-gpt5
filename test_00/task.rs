// 問題: 長方形について基本的な計算を行ってください。
// - 引数の width と height を受け取り、`let` で読みやすい変数を用意します。
// - 面積 (width * height)、周の長さ (2 * (width + height))、正方形かどうか (幅と高さが等しいか) を求めます。
// - 計算結果は (面積, 周の長さ, 正方形か) のタプルで返してください。
// - 面積・周の長さは不変なので `let`、必要に応じて中間値を更新する場合は `let mut` を使い分けましょう。
pub fn rectangle_report(width: u32, height: u32) -> (u32, u32, bool) {
    todo!("ここに処理を書いてください");
}
